import type { NextConfig } from "next";

// Pure SSG. No redirects (v1 was never publicly deployed), no middleware,
// no API routes — SPEC.md §5.
const nextConfig: NextConfig = {};

export default nextConfig;
